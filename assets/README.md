# Assets

- `assets/images/`: 일반 비트맵 이미지(`png`, `jpg`, `webp`)
- `assets/icons/`: 앱 아이콘성 이미지 에셋
- `assets/illustrations/`: 일러스트/상태 이미지

Flutter 등록은 `pubspec.yaml`에 이미 연결되어 있어서, 파일을 각 폴더에 추가한 뒤 `Image.asset(...)`으로 바로 사용할 수 있다.

